// testes/potencia.test.js
const potencia = require('../src/potencia');

test('2 elevado a 3 deve ser 8', () => {
  expect(potencia(2, 3)).toBe(8);
});

// adicione mais testes para a função potencia...
